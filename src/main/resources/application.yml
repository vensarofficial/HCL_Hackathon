spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ewalletdb
    username: postgres
    password: welcome
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        format_sql: true
    open-in-view: false
  jackson:
    time-zone: UTC

server:
  port: 8080
